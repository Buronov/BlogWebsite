<%- include("partials/header") -%>

    <h1>Home</h1>

    <p><%= startingContent %></p>

    <% posts.forEach((post) => { %>
      <a class="linkTitle" href="/posts/<%= post.title %>"><h1> <%= post.title %> </h1></a>
      <p> <%= post.content.substring(0,100) + " ..." %> <a href="/posts/<%= post.title %>">Read More</a> </p>

    <% }); %>


<%- include("partials/footer") -%>
